import { StandardizeResponseInterceptor } from './standardize-response.interceptor';

describe('StandardizeResponseInterceptor', () => {
  it('should be defined', () => {
    expect(new StandardizeResponseInterceptor()).toBeDefined();
  });
});
